# 計器のやりとり（横断の集約点）

> ★このフォルダが **計器・検査の知見をリポ間でやりとりする場所**です（2026-08-21 ユーザー指示）。
>
> ユーザーの言葉:
> 「追憶の煌めきのプログラムなので、**計器のやりとりはここにしたい**」
>
> 理由: 追憶（`tsuioku-no-kirameki.com`）は**1つのプロダクト**。
> 計器の知見は**プロダクトを越えて効く**ので、プロダクトの中に置くと
> 次に作るアプリへ届かない。★このキットは**新規アプリすべてに配られる**ので、
> ここに置けば自動的に横展開される。

---

## 1. いま配られている実体（コード）

| 実体 | 場所 | 状態 |
|---|---|---|
| 3値exit + 根拠なき緑の降格 + selftest | [`templates/scripts/lib/instrument-core.mjs`](../../templates/scripts/lib/instrument-core.mjs) | ★新規アプリすべてに同梱 |
| 使用例（毒→赤を実演） | [`templates/scripts/audit-native-cta.mjs`](../../templates/scripts/audit-native-cta.mjs) | `--selftest` あり |

★**コードの正本はキット側（このリポ）**。各プロダクトはコピーを持つ。

### ★コピー間の実測ドリフト（2026-08-21）

```
tsuioku の instrument-core.mjs  vs  キットの instrument-core.mjs
  → ヘッダのコメント 66行だけ違う（収穫元の書き分け）
  → ★実コードは 71行すべて一致（コメント除去して diff・確認済み）
```

★**プロダクト固有の事例をヘッダに書くのは正しい**（読む人が自分の事故で理解できる）。
ただし★**実コードが割れたら事故**なので、下の「掟」に照合手順を置く。

---

## 2. ★3値の終了コード（この土台の核心）

```
0 = 合格
1 = 測れた上での赤
★2 = 測れなかった   ← これを 0 と同じ緑に数えないこと
```

★**「何も測っていないのに合格」は赤より危険**（機能しているように見える）。

実損記録:
- `web-ios-android`（このリポ・2026-08-17）: `audit-native-cta.mjs` を引数なしで実行 →
  何も走査せず「✅ 0件」と表示（`fc3a8e3` で対処）
- `tsuioku-no-kirameki.com`（2026-08-21）: 検査53本のうち `--selftest` 0本・`exit 2` 0本

---

## 3. ★往復で確定した掟（実測に裏打ちされたものだけ）

### ① Detect the SHAPE, not one function name

> 出どころ: `soushin-suggest.link/scripts/audit-sentinel-holes.ps1:156`

★**名前だけを見る検査は「名前を変えて黙らせる」ことを誘う＝有害。**

実損（`tsuioku`・2026-08-21）:
```
audit-gates.mjs が raw（コメント込み）を見ていた
  → ★コメントに「直し方」と書くだけで ✔ が取れた
  → 実コードだけを見るよう修正 →  5/13本 → 11/13本
  ＝ ★6本が偽の緑だった
```

### ② ★「exit 2 を持っているか」は正しさの保証にならない

> 出どころ: `soushin-suggest.link`（2026-08-21 実測）

```
壊れていた3本のうち ★2本は exit 2 を持っていた
  それでも「測れなかった」を製品の赤として報告していた
```

★所有の検査（grep）は**入口**でしかない。持っていることと守れていることは別。

### ③ ★毒が入ったことを確認してから、赤かどうかを見る

★**「変異させた」と「変異が適用された」は別物。**

実損（`tsuioku`・2026-08-21・2回）:
```
検知器を壊して赤を確認しようとしたら「✅ OK」
  → ★置換が空振りしていて、毒が1文字も入っていなかった
  → 毒が入っていないのだから当然緑。それを「合格」と読みかけた
```

対処: 毒を入れた後に **grep で毒の存在を確認**してから結果を読む。
`soushin` 側は `Invoke-WithPoison` ヘルパで**置換が0件なら即エラー**にした
（作った当日に、作者自身の間違った置換パターンを止めた実績あり）。

### ④ ★方向は数字から推測できない

> 出どころ: `tsuioku`（changelog 1,349版を機械抽出した実データ）

```
0.1.887   100% → 0%     ★改善（エラー率が消えた）
0.1.1298  2回 → 13回    ★改善（描画が動くようになった）
0.1.1102  3秒 → 12秒    ★改善（取りこぼしを無くした）
```

★「小さいほど良い」を既定にしていたら、この3件を**全部「退化」と誤判定**していた。
＝ ★**正しく直した人を止める**。検査への信頼は一度で消える。

→ 方向（`better: 'lower' | 'higher'`）は**指標ごとに宣言**する。

### ⑤ ★「測れなかった」を表す値を、判定式に素通しさせない

同じ穴が**向き違いで両方**出た:

| リポ | 穴 | 結果 |
|---|---|---|
| `soushin` | `-1`（測れなかった）が `-le 0` を通る | ★偽の**赤** |
| `tsuioku` | `null` が `Number()` で `0` になる | ★偽の**緑** |

★`tsuioku` は同じ穴を**1日に4回**踏んだ（毎回テストが先に発見）。
対処: `typeof v === 'number'` で明示的に弾く。判定より前に「測れたか」を確かめる。

### ⑥ ★レポートにする。一括強制のゲートにしない

★このキットで死んだ仕掛けは全部「一度に全部直せ」と迫るもの。

```
✗「書かないと赤」   → 強制すると【嘘の数字】が入る
✅「間違って書くと赤」 → 退化・未宣言だけ止める（ベースライン＋ラチェット）
```

★実例: `tsuioku` の登録制の台帳は**3ヶ月で登録1件のまま死んだ**。

### ⑦ ★ラチェットは「値が悪化した」より「走っていない」で死ぬ

> 出どころ: `soushin-suggest.link`（2026-08-21・自リポの履歴を実測）

```
★8/8  : 実測25を24と書き、8日間ラチェットが赤のまま
★8/17 : コミット時に走らせ忘れ、また赤のまま
```

★どちらも「退化」ではなく★**「検査が走っていなかった」**。
しかも入口ゲートだったので、★**赤の間プローブが1本も走っていなかった**。

→ ★**3値規約の外側に「そもそも走っていない」という4つ目の状態がある**（未解決）。

---

## 4. 掟の照合（コピーが割れていないか）

```bash
# 実コードだけを比較する（コメントの書き分けは正常なので無視する）
node _docs/instruments/check-drift.mjs
```

★**コメントまで一致を要求しない**。要求すると、各リポが自分の事故を書けなくなる。

---

## 5. やりとりの作法

★手紙は相手リポの `_docs/` に `FROM-<送り主>-<件名>-<日付>.md` で置く。
**相手が読んで実際に直した実績がある**（`soushin` が `65a3e43` で応答）。

確定した往復（2026-08-21）:

```
soushin ──(3値規約・selftest・限界の明記)──→ tsuioku
tsuioku ──(SHAPE検査の穴・台帳・方向の宣言)──→ soushin
soushin ──(exit2は保証にならない・Invoke-WithPoison)──→ tsuioku
   ↓
★このキットへ集約 → 新規アプリすべてに配られる
```

★出どころをたどると**このキットの black-screen gate** に戻る（一周した）。

---

## 6. ★未解決（次に効くもの）

| # | 問い | 出どころ |
|---|---|---|
| 1 | ★「検査がそもそも走っていない」をどう検出するか（4つ目の状態） | `soushin` の実測2件 |
| 2 | 「exit 2 を持っているか」の代わりに**何を見れば**正しさに近づくか | `soushin` の実証 |
| 3 | 直した検査が**実機で緑のまま**か（`soushin` 側12本が未測定） | `soushin` 申告 |
